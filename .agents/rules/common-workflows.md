---
trigger: always_on
---

- Full workflow (sync, fix, test): `make all`
- Install dependencies: `make install`
- Install with dev dependencies: `make dev`
- Run app: `make run`
- Build rpm: `make rpm`
- Build flatpak `make flatpak`
